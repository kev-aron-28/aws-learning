1. A developer needs to store user session data for a web application. The solution must provide low latency, automatic scaling, and TTL-based expiration.
What is the best option?

B. Amazon DynamoDB with TTL

2. Your Lambda function needs to access secrets such as database credentials securely. What is the best approach?
B. Store them in AWS Secrets Manager and retrieve at runtime

3. An application uploads files to S3. You want to trigger processing immediately after upload. What should you use?
D. AWS Step Functions

4. A developer is debugging a distributed system and wants to trace requests across services. Which service should they use?
A. CloudWatch Logs

5. A Lambda function is timing out when accessing a database in a VPC. What is the most likely issue?
C. No NAT Gateway or VPC endpoint configured

6. Which services are event-driven? (Select TWO)
A. AWS Lambda
C. Amazon EventBridge

7. Which options help improve Lambda performance? (Select TWO)
A. Increase memory allocation
B. Use provisioned concurrency

8. Which are valid ways to grant permissions to AWS services? (Select TWO)
A. IAM Roles
B. Security Groups

9. You are building a microservices architecture. Services communicate asynchronously and must decouple failures. Which solution is best?
B. Amazon SQS

10. A developer needs to deploy a new version of a Lambda function gradually, shifting 10% of traffic first. What should they use?
B. Lambda Aliases with traffic shifting

11. Your application frequently reads the same data from DynamoDB. You want to reduce latency and cost. What should you do?
A. Increase read capacity

12. An application must process messages in order and avoid duplicates. Which service should be used?
A. SQS Standard Queue

13. AWS Lambda charges are based on execution time and memory used.
True

14. Amazon S3 provides strong consistency for all operations.
True

15. IAM roles can be assigned to EC2 instances.
True

16. You need to design a system that:
- Accepts file uploads
- Process them asynchronously
- Stores results in a database
- Scales automatically

Which AWS services would you use?
Well, I'd use a SQS queue to send all the jobs to be processed, of course in front of this queue must be an app or something to let users upload the file, like an EC2 this is in charge of sending the events.
To proccess the file, i guess we could go with a lambda, depending of course on the type of work you do, if its long term, then another EC2 as a worker listening to the JOBS and then save it to the DB

17. You are building an API with:
- Authentication
- Rate limiting
- Monitoring

What AWS services / features would you use?
Ill use an API gateway to get all this feature out of the box, of course, with its respective configuration


18. 