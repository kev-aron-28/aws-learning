1. You have an application running on EC2 instances behind an Application Load Balancer. Traffic suddenly increases and CPU utilization becomes very high.
What is the MOST AWS-native way to automatically add more servers?

B. Use an Auto Scaling Group

2. Which AWS service is mainly used to store objects like images, videos, backups, and static websites?l
C. S3

3. An EC2 instance in a private subnet needs internet access to download updates, but it should NOT accept inbound traffic from the internet.

What should you use?

B. NAT Gateway

4. What is the difference between Security Groups and Network ACLs?
B. Security Groups only allow rules

5. Which AWS database is BEST for highly scalable key-value workloads with single-digit millisecond latency?
C. DynamoDB

6. You upload a new version of your application code frequently and want a service that automatically deploys code to EC2 instances.
Which service helps with this?

A. CodeDeploy

7. A company wants to reduce latency globally for static assets like CSS, JS, and images.
Which service should they use?

B. CloudFront

8. What is the MAIN purpose of IAM Roles?
C. Grant temporary permissions to AWS services/users

9. Which service allows applications to send messages asynchronously between components?
A. SQS

10. An Application Load Balancer mainly operates at which OSI layer?
C. Layer 4

11. What happens if an EC2 instance is stopped?
B. RAM contents are lost

12. Which AWS service lets you run code WITHOUT managing servers?
C. Lambda

13. You want infrastructure to be defined using code files.
Which AWS service is specifically designed for this?

B. CloudFormation

14. Which Route53 routing policy distributes traffic across multiple resources?
C. Multivalue Routing

15. What is the MAIN advantage of using private subnets?
B. Resources are isolated from direct internet access

16. You are designing a highly available web application.
